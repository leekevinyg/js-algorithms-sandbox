interface TreeNode {
    val?: number,
    left?: TreeNode,
    right?: TreeNode,
}

export const maxPathSum = (root : TreeNode) => {
    /*   
       1) Find all possible paths
          - A possible path is where node B is reachable from Node A
          - In a binary tree, every node is reachable from every node

       2) 

       3) We must sum up each possible path and return the max sum.

    */
    
}